<style>
  /* Fragments transactional — aligned with app theme (blue + amber) */
  body.email-body {
    margin: 0;
    padding: 32px 16px;
    background-color: #e0f2fe;
    -webkit-font-smoothing: antialiased;
  }
  .email-wrap {
    max-width: 560px;
    margin: 0 auto;
  }
  .email-card {
    background: #ffffff;
    border-radius: 12px;
    overflow: hidden;
    border: 1px solid #bfdbfe;
    box-shadow: 0 4px 24px rgba(30, 64, 175, 0.1);
  }
  .email-accent-strip {
    height: 4px;
    background: linear-gradient(90deg, #1e40af 0%, #3b82f6 50%, #f59e0b 100%);
  }
  .email-inner {
    padding: 28px 28px 32px;
  }
  .email-brand {
    font-size: 12px;
    font-weight: 700;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    color: #1e40af;
    margin: 0 0 4px;
    text-align: center;
  }
  .email-title {
    font-size: 22px;
    font-weight: 700;
    color: #1e293b;
    margin: 0 0 20px;
    line-height: 1.3;
    text-align: center;
  }
  .email-p {
    font-size: 15px;
    line-height: 1.6;
    color: #334155;
    margin: 0 0 12px;
    text-align: center;
  }
  .email-p-left {
    text-align: left;
  }
  .email-cta-wrap {
    text-align: center;
    margin: 28px 0 8px;
  }
  .email-btn {
    display: inline-block;
    background: #f59e0b;
    color: #ffffff !important;
    font-weight: 600;
    font-size: 15px;
    padding: 14px 28px;
    border-radius: 8px;
    text-decoration: none !important;
    border: 1px solid #d97706;
    box-shadow: 0 2px 4px rgba(180, 83, 9, 0.2);
  }
  .email-btn:hover {
    background: #d97706;
  }
  .email-footer {
    font-size: 13px;
    line-height: 1.5;
    color: #64748b;
    margin-top: 24px;
    padding-top: 20px;
    border-top: 1px solid #e2e8f0;
    text-align: center;
  }
  .email-box {
    margin-top: 16px;
    padding: 14px 16px;
    background: #f8fafc;
    border: 1px solid #e2e8f0;
    border-radius: 8px;
    text-align: left;
    font-size: 14px;
    color: #334155;
    line-height: 1.5;
  }
  .email-table {
    width: 100%;
    border-collapse: collapse;
    margin-top: 16px;
    font-size: 14px;
  }
  .email-table th,
  .email-table td {
    border: 1px solid #e2e8f0;
    padding: 12px 14px;
    text-align: left;
    vertical-align: top;
  }
  .email-table th {
    background-color: #eff6ff;
    color: #1e40af;
    font-weight: 600;
    width: 120px;
  }
  .email-table td {
    color: #334155;
  }
</style>
